apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: client_service
  name: client_service
  labels:
    app: client_service
spec:
  selector:
    matchLabels:
      app: client_service
  template:
    metadata:
      labels:
        app: client_service
        name: client_service
    spec:
      containers:
        - name: client_service
          image: pavan-intelops/user_management/client_service
          imagePullPolicy: Always
        
          env:
            - name: SERVICE_NAME
              value: "go-app"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "localhost:4317"
            - name: INSECURE_MODE
              value: "true"
        
          ports:
        
        
          - containerPort: 4000
            name: http
        
        
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 5
            periodSeconds: 15
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 15
            periodSeconds: 30
        